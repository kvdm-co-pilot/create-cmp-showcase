# Spec: favorites

> Generated by `scaffold-feature.mjs` from the `home` exemplar shape. Refine the clause
> prose below for Favorites's real behavior (ids stay fixed) before running the verify lane.

- **FAVORITES-01** — Given the Favorites screen opens, When favorites are being loaded, Then a loading
  indicator is shown and no favorites are visible.
- **FAVORITES-02** — Given the repository returns favorites, When loading completes, Then the favorites
  are listed with their title and subtitle, and no error is shown.
- **FAVORITES-03** — Given the repository fails, When loading completes, Then a human-readable
  error message is shown (`favorites_error`) and no favorites are visible.
- **FAVORITES-04** — Given a load has failed, When the data source recovers and the user
  triggers a reload, Then the error clears and the favorites render.
- **FAVORITES-05** — Given favorites are listed, When the user taps an item, Then the app navigates
  to that item's detail.
- **FAVORITES-06** — Given the Favorites screen renders, When its structure is inspected, Then the
  screen matches its committed golden tree (`qa/golden/favorites.json`) — structural regressions
  are intentional, declared changes only.

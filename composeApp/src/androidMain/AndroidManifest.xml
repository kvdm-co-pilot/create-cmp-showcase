<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      PLAN-07. USE_EXACT_ALARM is deliberately NOT declared: it is reserved for alarm-clock and
      calendar apps, and Play rejects it for a meal reminder. SCHEDULE_EXACT_ALARM is the
      user-grantable one — when it is not granted the scheduler arms windowed-inexact alarms
      instead, which is the normal path here, not an error path.
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Alarms do not survive a reboot; the boot receiver below re-arms them. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".AppApplication"
        android:label="Fuelled"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="${usesCleartextTraffic}"
        android:theme="@style/Theme.App">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          PLAN-07. Both exported="false": the alarm receiver is triggered only by our own
          PendingIntents, and BOOT_COMPLETED is delivered to non-exported receivers by the
          system anyway. Nothing outside the app has any business posting these.
        -->
        <receiver
            android:name=".notification.MealReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".notification.BootReArmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>

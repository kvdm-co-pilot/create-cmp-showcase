<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      PLAN-07. USE_EXACT_ALARM is deliberately NOT declared: it is reserved for alarm-clock and
      calendar apps, and Play rejects it for a meal reminder. SCHEDULE_EXACT_ALARM is the
      user-grantable one — when it is not granted the scheduler arms windowed-inexact alarms
      instead, which is the normal path here, not an error path.
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Alarms do not survive a reboot; the boot receiver below re-arms them. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!--
      UPD-06. Fuelled is sideloaded (never Play — Play's Device and Network Abuse policy bars an
      app from installing its own updates), so it hands the downloaded APK to the system
      installer and the user confirms there. Without this the hand-off is refused outright.
      The user must ALSO grant "install unknown apps" per-app; that is a runtime state the
      surface checks before spending data on a download (E4).
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".AppApplication"
        android:label="Fuelled"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="${usesCleartextTraffic}"
        android:theme="@style/Theme.App">

        <!--
          UPD-06: the authority AppInstaller builds its content:// URI with
          ("${'$'}{packageName}.updates"). A file:// URI has been illegal to pass across an app
          boundary since API 24 (FileUriExposedException), so the installer cannot read the APK
          without this. Scoped to cache/ alone — see res/xml/update_paths.xml.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updates"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/update_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          PLAN-07. Both exported="false": the alarm receiver is triggered only by our own
          PendingIntents, and BOOT_COMPLETED is delivered to non-exported receivers by the
          system anyway. Nothing outside the app has any business posting these.
        -->
        <receiver
            android:name=".notification.MealReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".notification.BootReArmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
